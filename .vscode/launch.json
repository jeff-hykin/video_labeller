{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type":"node",
            "request": "launch",
            "args": ["--extensionDevelopmentPath=${workspaceRoot}"],
            "preLaunchTask": "build"
        },
    ]
}
