import Vue from "vue"

import { barMeasure } from '@/components/bar-measure'

export default {
    data() {
        return {
            featureManager: {
                // 
                // data
                // 
                featureValue: 0,
            },
        }
    },
    watch: {
    },
    mounted() {
        console.log(`blah`)
    },
}
